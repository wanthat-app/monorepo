{
  "name": "@wanthat/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "synth": "cdk synth",
    "diff": "cdk diff --all",
    "deploy": "cdk deploy --all --require-approval never --concurrency 4",
    "deploy:dev": "cdk deploy --all --require-approval never --concurrency 4 --hotswap-fallback",
    "test": "echo \"no tests yet\""
  },
  "dependencies": {
    "@wanthat/config": "workspace:*",
    "aws-cdk-lib": "^2.260.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@wanthat/contracts": "workspace:*",
    "@wanthat/db": "workspace:*",
    "@wanthat/domain": "workspace:*",
    "@wanthat/dynamo": "workspace:*",
    "@wanthat/web": "workspace:*",
    "aws-cdk": "^2.150.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.0"
  }
}
