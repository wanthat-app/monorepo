# @wanthat/web

The Next.js/React SPA (web-first MVP — desktop + mobile web). Hosted on S3 behind
CloudFront. Consumes `@wanthat/contracts` for shared types/validation.

Scaffolding TBD — initialize the Next.js app here (`pnpm create next-app .`) when web work begins.
