{
  "name": "@wanthat/retailer-proxy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.8.0",
    "@wanthat/aliexpress": "workspace:*",
    "@wanthat/config": "workspace:*",
    "@wanthat/contracts": "workspace:*"
  }
}
